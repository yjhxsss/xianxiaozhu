/* ================================================================
 * 应用版本号 —— 每次发版改这里
 * ================================================================ */
export const APP_VERSION = '4.1.0';
export const BUILD_DATE = '2026-09-18';

/* 数据格式版本号 —— 如果 DB 结构变了需要写迁移，改这里 */
export const SCHEMA_VERSION = 2;

/* 本地存储 key（避免和其他应用冲突） */
export const STORAGE_PREFIX = 'xxz_';
// END OF FILE